import { useEffect, useMemo, useState } from "react";
import type { ThreeDayMeeting, ThreeDayRace } from "../services/threeDayCatalog";
import { canonicalRailDisplay, canonicalTrackDisplayName, canonicalTrackRatingDisplay, canonicalWeatherDisplay, cleanProductText } from "../../design-system/presentation";
import { loadMeetingDetail, loadMeetingsWorkspaceViewModel, type MeetingSummaryViewModel, type MeetingsDayKey, type MeetingsWorkspaceViewModel } from "../services/meetingsFeed";
import { loadRaceDetail } from "../services/raceDetailFeed";
import { loadRacecourseWeatherFeed, meetingWeather, weatherDescription, type RacecourseWeatherRow } from "../services/racecourseWeatherFeed";
type MeetingsWorkspaceProps={selectedDayKey:MeetingsDayKey;selectedMeetingKey:string|null;clean:(value:any)=>string;onDayChange:(day:MeetingsDayKey)=>void;onSelectMeeting:(meeting:ThreeDayMeeting|null)=>void;onOpenMeeting:(meeting:ThreeDayMeeting)=>void;onOpenRace:(meeting:ThreeDayMeeting,race:ThreeDayRace,index:number)=>void};type Jurisdiction="VIC"|"WA"|"HK"|"OTHER";const display=(value:unknown,fallback="—")=>cleanProductText(value,fallback);const JURISDICTIONS:Record<Jurisdiction,{name:string;authority:string;code:string}>={VIC:{name:"Victoria",authority:"Racing Victoria",code:"RV"},WA:{name:"Western Australia",authority:"Racing WA",code:"RWA"},HK:{name:"Hong Kong",authority:"Hong Kong Jockey Club",code:"HKJC"},OTHER:{name:"Other",authority:"Racing Authority",code:"PRA"}};
function jurisdictionOf(m:MeetingSummaryViewModel):Jurisdiction{const state=display(m.state,"").toUpperCase(),name=canonicalTrackDisplayName(m.meeting).toUpperCase();if(state==="VIC"||state.includes("VICTORIA"))return"VIC";if(state==="WA"||state.includes("WESTERN AUSTRALIA"))return"WA";if(state==="HK"||state.includes("HONG KONG")||/SHA TIN|HAPPY VALLEY/.test(name))return"HK";return"OTHER"}function shortDate(v:string){const[y,m,d]=v.split("-").map(Number);if(!y||!m||!d)return v;return new Intl.DateTimeFormat("en-AU",{weekday:"short",day:"numeric",month:"short",timeZone:"Australia/Melbourne"}).format(new Date(Date.UTC(y,m-1,d)))}function tabTitle(i:number){return i===0?"Today":i===1?"Tomorrow":"Day +2"}
export function MeetingsWorkspace({selectedDayKey,selectedMeetingKey,onDayChange,onSelectMeeting,onOpenMeeting,onOpenRace}:MeetingsWorkspaceProps){const[model,setModel]=useState<MeetingsWorkspaceViewModel|null>(null),[loading,setLoading]=useState(true),[detailLoading,setDetailLoading]=useState(false),[loadError,setLoadError]=useState(""),[search,setSearch]=useState(""),[jurisdictionFilter,setJurisdictionFilter]=useState<"ALL"|Jurisdiction>("ALL"),[details,setDetails]=useState<Record<string,ThreeDayMeeting>>({}),[weatherRows,setWeatherRows]=useState<RacecourseWeatherRow[]>([]);
useEffect(()=>{let active=true;Promise.all([loadMeetingsWorkspaceViewModel(),loadRacecourseWeatherFeed().catch(()=>[])]).then(([next,w])=>{if(active){setModel(next);setWeatherRows(w);setLoading(false);setLoadError("")}}).catch(e=>{if(active){setModel(null);setLoading(false);setLoadError(e instanceof Error?e.message:"Meetings feed unavailable")}});return()=>{active=false}},[]);const activeDay=model?.days.find(d=>d.key===selectedDayKey)??model?.days[0]??null;
useEffect(()=>{let active=true;if(!activeDay)return;Promise.all(activeDay.meetings.map(async m=>{try{return[m.meetingKey,await loadMeetingDetail(activeDay.date,m.meetingKey)] as const}catch{return null}})).then(rows=>{if(active)setDetails(Object.fromEntries(rows.filter((x):x is readonly[string,ThreeDayMeeting]=>Boolean(x))))});return()=>{active=false}},[activeDay]);
const meetings=useMemo(()=>{const q=search.trim().toLowerCase();return(activeDay?.meetings??[]).filter(m=>{const j=jurisdictionOf(m),name=canonicalTrackDisplayName(m.meeting).toLowerCase();return(!q||name.includes(q))&&(jurisdictionFilter==="ALL"||j===jurisdictionFilter)})},[activeDay,search,jurisdictionFilter]);const grouped=useMemo(()=>(["VIC","WA","HK","OTHER"] as Jurisdiction[]).map(key=>({key,meetings:meetings.filter(m=>jurisdictionOf(m)===key)})).filter(g=>g.meetings.length>0),[meetings]);
async function openMeeting(m:MeetingSummaryViewModel){setDetailLoading(true);try{const x=await loadMeetingDetail(m.rawMeeting.date,m.meetingKey);onSelectMeeting(x);onOpenMeeting(x)}catch(e){setLoadError(e instanceof Error?e.message:"Meeting detail unavailable")}finally{setDetailLoading(false)}}async function openFirstRace(m:MeetingSummaryViewModel){const r=m.raceSummaries[0];if(!r)return void openMeeting(m);setDetailLoading(true);try{const[md,rd]=await Promise.all([loadMeetingDetail(m.rawMeeting.date,m.meetingKey),loadRaceDetail(m.rawMeeting.date,m.meetingKey,r.raceKey)]);const merged={...md,races:md.races.map(x=>x.raceKey===r.raceKey?rd:x)};onSelectMeeting(merged);onOpenRace(merged,rd,0)}catch(e){setLoadError(e instanceof Error?e.message:"Race detail unavailable")}finally{setDetailLoading(false)}}
if(loading)return <section className="eiq-meetings-locked"><div className="eiq-meetings-locked__empty">Loading meetings…</div></section>;return <section className="eiq-meetings-locked" aria-label="Meetings" data-edgeiq-workspace-key="MEETINGS"><header className="eiq-meetings-locked__header"><div><h1>Meetings</h1>{loadError?<p role="alert">{loadError}</p>:null}</div></header><div className="eiq-meetings-locked__days">{(model?.days??[]).slice(0,3).map((day,i)=><button key={day.key} className={day.key===activeDay?.key?"is-active":""} onClick={()=>onDayChange(day.key)}><strong>{tabTitle(i)}</strong><span>{shortDate(day.date)}</span></button>)}</div><section className="eiq-meetings-locked__filters"><div className="eiq-meetings-jurisdiction-filter"><span>Show</span>{(["ALL","VIC","WA","HK"] as const).map(key=><button key={key} className={jurisdictionFilter===key?"is-active":""} onClick={()=>setJurisdictionFilter(key)}>{key==="ALL"?"All":key}</button>)}</div><label className="is-search">Search meetings<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search meetings…"/></label></section><div className="eiq-meetings-jurisdictions">{grouped.map(({key,meetings:ms})=>{const meta=JURISDICTIONS[key];return <section className="eiq-meetings-panel eiq-meetings-jurisdiction" key={key}><header className="eiq-meetings-jurisdiction__header"><div className="eiq-pra-identity"><span className="eiq-pra-identity__fallback">{meta.code}</span><div><strong>{meta.name}</strong><small>{meta.authority}</small></div></div><span>{ms.length} meeting{ms.length===1?"":"s"} · {ms.reduce((s,m)=>s+m.races,0)} races</span></header><div className="eiq-meetings-locked__table-wrap"><table className="eiq-meetings-locked__table"><thead><tr><th>Meeting</th><th>State</th><th>Rail</th><th>Track Condition</th><th>Weather</th><th>First Race</th><th>Races</th><th>Scratchings</th><th/></tr></thead><tbody>{ms.map(m=>{const detail=details[m.meetingKey],first=detail?.races?.[0]?.raceTime||m.first,rail=detail?.rail||m.rail,track=detail?.trackCondition||m.track,w=meetingWeather(weatherRows,m.meetingKey,activeDay?.date);return <tr key={m.meetingKey} className={m.meetingKey===selectedMeetingKey?"is-selected":""} onClick={()=>onSelectMeeting(detail??m.rawMeeting)}><td><strong>{canonicalTrackDisplayName(m.meeting)}</strong></td><td>{display(m.state)}</td><td>{canonicalRailDisplay(rail)}</td><td>{canonicalTrackRatingDisplay(track)}</td><td>{w?weatherDescription(w):canonicalWeatherDisplay(m.weather)}</td><td>{display(first)}</td><td>{m.races}</td><td>{m.scratchings??0}</td><td><button disabled={detailLoading} onClick={e=>{e.stopPropagation();void openFirstRace(m)}}>View Races →</button></td></tr>})}</tbody></table></div></section>})}</div></section>}
