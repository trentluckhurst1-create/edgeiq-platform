import { useEffect, useMemo, useState } from "react";
import { CalendarDays, ChevronRight, CloudSun, FileText, Gauge, Lightbulb, LineChart, Map, MapPin, Search, Trophy, Users } from "lucide-react";
import { loadMeetingsWorkspaceViewModel, type MeetingsWorkspaceViewModel } from "../race/services/meetingsFeed";
import { canonicalTrackDisplayName, cleanProductText } from "../design-system/presentation";
type Props={onOpenMeetings?:()=>void};const d=(v:unknown,f="—")=>cleanProductText(v,f);
function dayLabel(i:number,v:string|Date){const x=v instanceof Date?v:new Date(v);return{title:i===0?"Today":i===1?"Tomorrow":"Day +2",date:Number.isNaN(x.getTime())?String(v):new Intl.DateTimeFormat("en-AU",{day:"numeric",month:"short",timeZone:"Australia/Melbourne"}).format(x)}}
function fallbacks(){const n=new Date();return[0,1,2].map(i=>{const x=new Date(n);x.setDate(n.getDate()+i);return{key:`F${i}`,displayDate:x}})}
export function EdgeiqOsHome({onOpenMeetings}:Props){const[model,setModel]=useState<MeetingsWorkspaceViewModel|null>(null);useEffect(()=>{let live=true;loadMeetingsWorkspaceViewModel().then(v=>live&&setModel(v)).catch(()=>live&&setModel(null));return()=>{live=false}},[]);const days=model?.days.slice(0,3)??[];const tabs=days.length?days.map(x=>({key:x.key,displayDate:x.displayDate,meetings:x.totals.meetings,races:x.totals.races})):fallbacks().map(x=>({...x,meetings:0,races:0}));const today=days[0];const meetings=useMemo(()=>today?.meetings??[],[today]);const runners=today?.totals.declared??0;const races=today?.totals.races??0;const scratches=today?.totals.scratchings??0;const tools=[{label:"Form",Icon:FileText},{label:"Performance",Icon:LineChart},{label:"Map",Icon:Map},{label:"Market",Icon:Gauge},{label:"Nexus",Icon:LineChart},{label:"Track Details",Icon:MapPin},{label:"Weather",Icon:CloudSun},{label:"Insights",Icon:Lightbulb}];return <section className="eiq-home-mock" data-edgeiq-workspace-key="HOME"><div className="eiq-home-search"><Search size={17}/><span>Search horses, trainers, jockeys, meetings...</span></div><section className="eiq-home-hero"><div><span>Welcome to</span><strong>EDGE<span>iQ</span></strong><b>RACING INTELLIGENCE</b><p>Deeper data. Sharper insight. A professional edge.</p></div><aside><h2>Data. Form. Context. Edge.</h2><p>Built for professionals.</p></aside></section><div className="eiq-home-days">{tabs.map((x,i)=>{const l=dayLabel(i,x.displayDate);return <button key={x.key} onClick={onOpenMeetings}><CalendarDays size={29}/><div><small>{l.title}</small><strong>{x.meetings} {x.meetings===1?"Meeting":"Meetings"}</strong><span>{x.races} Races · {l.date}</span></div><ChevronRight size={20}/></button>})}</div><div className="eiq-home-layout"><main><section className="eiq-home-card"><header><h2><CalendarDays size={18}/> Today’s Meetings</h2><button onClick={onOpenMeetings}>View All Meetings →</button></header><table><thead><tr><th>Meeting</th><th>State</th><th>Races</th><th>First Race</th><th>Rail</th><th>Track</th><th>Weather</th><th>Scratchings</th><th/></tr></thead><tbody>{meetings.map(m=><tr key={m.meetingKey}><td><strong>{canonicalTrackDisplayName(m.meeting)}</strong></td><td>{d(m.state)}</td><td>{m.races}</td><td>{d(m.first)}</td><td>{d(m.rail)}</td><td><b className="eiq-track-pill">{d(m.track)}</b></td><td>{d(m.weather)}</td><td>{m.scratchings??0}</td><td><button onClick={onOpenMeetings}>View Races →</button></td></tr>)}</tbody></table></section><div className="eiq-home-bottom"><section className="eiq-home-card"><header><h2><Trophy size={18}/> Recent Results</h2></header><div className="eiq-home-placeholder">Results populate from the current results feed.</div></section><section className="eiq-home-card"><header><h2><LineChart size={18}/> Top Performers <small>(Last 5 Runs)</small></h2></header><div className="eiq-home-placeholder">Performance rankings populate from current runner history.</div></section></div></main><aside className="eiq-home-right"><section className="eiq-home-card"><header><h2>Today’s Quick Stats</h2></header><div className="eiq-quick-stats"><article><Users size={24}/><span>Runners</span><strong>{runners}</strong></article><article><Gauge size={24}/><span>Races</span><strong>{races}</strong></article><article><Trophy size={24}/><span>Meetings</span><strong>{meetings.length}</strong></article><article><FileText size={24}/><span>Scratchings</span><strong>{scratches}</strong></article></div></section><section className="eiq-home-card"><header><h2>Popular Tools</h2></header><div className="eiq-tools">{tools.map(({label,Icon})=><button key={label} onClick={onOpenMeetings}><Icon size={19}/><span>{label}</span><ChevronRight size={15}/></button>)}</div></section><section className="eiq-home-card eiq-home-insights"><header><h2><Lightbulb size={18}/> Insights</h2></header>{meetings.slice(0,4).map(m=><div key={m.meetingKey}><i/> <span><strong>{canonicalTrackDisplayName(m.meeting)}</strong> · {d(m.track)} · {m.races} races</span></div>)}</section></aside></div></section>}
