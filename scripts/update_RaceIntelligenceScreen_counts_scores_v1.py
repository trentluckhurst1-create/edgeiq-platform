import csv
from pathlib import Path
from datetime import datetime
ROOT=Path(__file__).resolve().parents[1]; DATA=ROOT/'public'/'data'; TSX=ROOT/'src'/'components'/'RaceIntelligenceScreen.tsx'
out=DATA/'edgeiq_counts_scores_ui_update_v1.csv'; sumout=DATA/'edgeiq_counts_scores_ui_update_v1_summary.csv'; report=DATA/'edgeiq_counts_scores_ui_update_v1_report.txt'
status='COUNTS_SCORES_UI_UPDATED'; err=''; changes=[]
def wcsv(path,row):
    with path.open('w', newline='', encoding='utf-8') as f:
        w=csv.DictWriter(f, fieldnames=list(row.keys())); w.writeheader(); w.writerow(row)
try:
    text=TSX.read_text(encoding='utf-8')
    original=text
    def rep(old,new,label,required=True):
        global text
        nonlocal_changes=None
        if old in text:
            text=text.replace(old,new,1); changes.append(label); return True
        if required and new not in text:
            raise RuntimeError(f'Missing replacement target: {label}')
        return False
    rep('  opportunityScore: "/data/edgeiq_opportunity_score_v1.csv",\n};', '  opportunityScore: "/data/edgeiq_opportunity_score_v1.csv",\n  commandEnrichment: "/data/edgeiq_command_enrichment_feed_v2.csv",\n};', 'files_command_enrichment_v2')
    rep('  hiddenGem?: Row;\n};', '  hiddenGem?: Row;\n  commandEnrichment?: Row;\n};', 'type_command_enrichment')
    rep('  const [opportunityScoreRows, setOpportunityScoreRows] = useState<Row[]>([]);', '  const [opportunityScoreRows, setOpportunityScoreRows] = useState<Row[]>([]);\n  const [commandEnrichmentRows, setCommandEnrichmentRows] = useState<Row[]>([]);', 'state_command_enrichment')
    rep('const [runner, runnerIntel, v8, bet, rel, cards, briefing, marketIntel, verdict, trackIntel, horseDrawer, runnerDnaDrawer, explainability, connection, factorScorecard, limitedData, intelligenceScore, customerIntelligence, intelligenceSummary, raceDayIntelligence, runnerProfile, formIntelligence, runnerForm, runnerFormHistory, historyMaster, historyDetail, horseCareer, horseArchetype, horseTrajectory, horseProjection, campaignIntelligence, hiddenGem, raceShapeFallback, chaosIndex, opportunityScore] = await Promise.all([', 'const [runner, runnerIntel, v8, bet, rel, cards, briefing, marketIntel, verdict, trackIntel, horseDrawer, runnerDnaDrawer, explainability, connection, factorScorecard, limitedData, intelligenceScore, customerIntelligence, intelligenceSummary, raceDayIntelligence, runnerProfile, formIntelligence, runnerForm, runnerFormHistory, historyMaster, historyDetail, horseCareer, horseArchetype, horseTrajectory, horseProjection, campaignIntelligence, hiddenGem, raceShapeFallback, chaosIndex, opportunityScore, commandEnrichment] = await Promise.all([', 'promise_destructure_command_enrichment')
    rep('        loadCsv(FILES.opportunityScore),\n      ]);', '        loadCsv(FILES.opportunityScore),\n        loadCsv(FILES.commandEnrichment),\n      ]);', 'promise_load_command_enrichment')
    rep('      setOpportunityScoreRows(opportunityScore);\n      setIntelligenceScoreRows(intelligenceScore);', '      setOpportunityScoreRows(opportunityScore);\n      setCommandEnrichmentRows(commandEnrichment);\n      setIntelligenceScoreRows(intelligenceScore);', 'set_command_enrichment')
    rep('      const hiddenGem = findHiddenGemForRunner(hiddenGemRows, row);\n      const runnerHistoryMaster', '      const hiddenGem = findHiddenGemForRunner(hiddenGemRows, row);\n      const commandEnrichment = findSidecar(commandEnrichmentRows, row);\n      const runnerHistoryMaster', 'find_command_enrichment')
    rep('return { row, runnerIntel, v8, bet, rel, drawer, dna, explainability, connection, limited, intel, customerIntel, intelligenceSummary, raceDayIntelligence, runnerProfile, formIntelligence, runnerForm, runnerHistory, runnerCareer, runnerArchetype, runnerTrajectory, runnerProjection, campaign, hiddenGem, factorRows };', 'return { row, runnerIntel, v8, bet, rel, drawer, dna, explainability, connection, limited, intel, customerIntel, intelligenceSummary, raceDayIntelligence, runnerProfile, formIntelligence, runnerForm, runnerHistory, runnerCareer, runnerArchetype, runnerTrajectory, runnerProjection, campaign, hiddenGem, commandEnrichment, factorRows };', 'return_command_enrichment')
    rep('campaignIntelligenceRows, hiddenGemRows]);', 'campaignIntelligenceRows, hiddenGemRows, commandEnrichmentRows]);', 'deps_command_enrichment')
    # Add helper functions after hasMergedEvidencePayload if missing.
    helper_marker='''function hasMergedEvidencePayload(row: Row | undefined): boolean {\n  return evidenceFlag(row, [\n    "edgeiq_connection_evidence_available",\n    "edgeiq_market_evidence_available",\n    "edgeiq_hidden_gem_evidence_available",\n  ]);\n}\n\n'''
    helper_insert=helper_marker+'''function usefulEvidenceText(value: string): boolean {\n  const normalized = value.trim().toUpperCase();\n  return !!normalized && !["--", "NO", "NONE", "N/A", "NA", "NO_SOURCE_MATCH", "NO_CONNECTION", "NO_EVIDENCE", "FALSE", "0", "NO CONNECTION ANGLE TRIGGERED.", "NO MARKET SIGNAL LOADED.", "NO HIDDEN GEM FLAGGED."].includes(normalized);\n}\n\nfunction commandEvidenceSource(item: EnrichedRunner): Row | undefined {\n  return item.commandEnrichment || item.row;\n}\n\nfunction commandEvidenceAvailable(item: EnrichedRunner, flagKeys: string[], summaryKeys: string[]): boolean {\n  const source = commandEvidenceSource(item);\n  if (evidenceFlag(source, flagKeys)) return true;\n  return usefulEvidenceText(firstText(source, summaryKeys, ""));\n}\n\nfunction scoreToneValue(value: number | null): string {\n  if (value === null || !Number.isFinite(value)) return "#64748b";\n  if (value >= 80) return "#34d399";\n  if (value >= 60) return "#7dd3fc";\n  if (value >= 40) return "#f5c451";\n  return "#f87171";\n}\n\n'''
    rep(helper_marker, helper_insert, 'command_evidence_helpers')
    rep('  const connectionsCoverageCount = coverageCount((row) => row.connectionLoaded || evidenceFlag(row.item.row, ["edgeiq_connection_evidence_available"]));\n  const marketCoverageCount = coverageCount((row) => evidenceFlag(row.item.row, ["edgeiq_market_evidence_available"]));\n  const hiddenGemCoverageCount = coverageCount((row) => evidenceFlag(row.item.row, ["edgeiq_hidden_gem_evidence_available"]));', '  const connectionsCoverageCount = coverageCount((row) => commandEvidenceAvailable(row.item, ["edgeiq_connection_evidence_available_v2", "edgeiq_connection_evidence_available"], ["edgeiq_connection_angle_summary_v2", "edgeiq_connection_angle_summary"]));\n  const marketCoverageCount = coverageCount((row) => commandEvidenceAvailable(row.item, ["edgeiq_market_evidence_available_v2", "edgeiq_market_evidence_available"], ["edgeiq_market_signal_summary_v2", "edgeiq_market_signal_summary"]));\n  const hiddenGemCoverageCount = coverageCount((row) => commandEvidenceAvailable(row.item, ["edgeiq_hidden_gem_evidence_available_v2", "edgeiq_hidden_gem_evidence_available"], ["edgeiq_hidden_gem_summary_v2", "edgeiq_hidden_gem_summary"]));', 'coverage_counts_v2')
    rep('''          const marketCommandRows = controlRunners\n            .map((item) => {\n              const activeFair = firstNum(item.row, ["edgeiq_v7_2g2_active_display_fair_price_shadow", "edgeiq_v7_2g2_on_preview_display_fair_price", "edgeiq_active_display_fair_price", "fair_price", "ui_fair_price"]);\n              const liveReference = livePrice(item.row, item.bet);\n              const edge = activeFair !== null && liveReference !== null && activeFair > 0\n                ? ((liveReference / activeFair) - 1) * 100\n                : edgePct(item.row, item.bet);\n              const hasMarketEvidence = evidenceFlag(item.row, ["edgeiq_market_evidence_available"]);\n              const summary = firstText(item.row, ["edgeiq_market_signal_summary"], "");\n              const role = edge !== null && edge >= 15\n                ? "BEST VALUE"\n                : edge !== null && edge <= -15\n                  ? "OVERBET RISK"\n                  : hasMarketEvidence\n                    ? "MARKET WATCH"\n                    : "NO MARKET SIGNAL";\n              return { item, activeFair, liveReference, edge, hasMarketEvidence, summary, role };\n            })\n            .filter((entry) => entry.hasMarketEvidence || entry.activeFair !== null || entry.liveReference !== null);''', '''          const marketCommandRows = controlRunners\n            .map((item) => {\n              const source = commandEvidenceSource(item);\n              const activeFair = firstNum(source, ["edgeiq_active_display_fair_price", "edgeiq_v7_2g2_guarded_display_fair_price", "edgeiq_v7_2g2_active_display_fair_price_shadow", "edgeiq_v7_2g2_on_preview_display_fair_price"]) ?? firstNum(item.row, ["edgeiq_v7_2g2_active_display_fair_price_shadow", "edgeiq_v7_2g2_on_preview_display_fair_price", "fair_price", "ui_fair_price"]);\n              const liveReference = livePrice(item.row, item.bet);\n              const edge = activeFair !== null && liveReference !== null && activeFair > 0\n                ? ((liveReference / activeFair) - 1) * 100\n                : edgePct(item.row, item.bet);\n              const hasMarketEvidence = commandEvidenceAvailable(item, ["edgeiq_market_evidence_available_v2", "edgeiq_market_evidence_available"], ["edgeiq_market_signal_summary_v2", "edgeiq_market_signal_summary"]);\n              const summary = firstText(source, ["edgeiq_market_signal_summary_v2", "edgeiq_market_signal_summary"], "");\n              const role = firstText(source, ["command_market_role_v2", "command_market_role"], "") || (edge !== null && edge >= 15\n                ? "BEST VALUE"\n                : edge !== null && edge <= -15\n                  ? "OVERBET RISK"\n                  : hasMarketEvidence\n                    ? "MARKET WATCH"\n                    : "NO MARKET SIGNAL");\n              return { item, activeFair, liveReference, edge, hasMarketEvidence, summary, role };\n            })\n            .filter((entry) => entry.hasMarketEvidence || entry.activeFair !== null || entry.liveReference !== null);''', 'market_command_v2')
    rep('''          const connectionCommandRows = controlRunners\n            .map((item) => ({\n              item,\n              hasConnectionEvidence: evidenceFlag(item.row, ["edgeiq_connection_evidence_available"]),\n              summary: firstText(item.row, ["edgeiq_connection_angle_summary"], ""),\n              score: connectionScoreValue(item),\n            }))\n            .filter((entry) => entry.hasConnectionEvidence);''', '''          const connectionCommandRows = controlRunners\n            .map((item) => {\n              const source = commandEvidenceSource(item);\n              return {\n                item,\n                hasConnectionEvidence: commandEvidenceAvailable(item, ["edgeiq_connection_evidence_available_v2", "edgeiq_connection_evidence_available"], ["edgeiq_connection_angle_summary_v2", "edgeiq_connection_angle_summary"]),\n                summary: firstText(source, ["edgeiq_connection_angle_summary_v2", "edgeiq_connection_angle_summary"], ""),\n                score: firstNum(source, ["edgeiq_score_connections_v2"]) ?? connectionScoreValue(item),\n              };\n            })\n            .filter((entry) => entry.hasConnectionEvidence);''', 'connection_command_v2')
    rep('''          const scoreBreakdownRows = scoreBreakdownRunner\n            ? [\n                { label: "Overall", value: firstNum(scoreBreakdownRunner.row, ["total_rating_points", "runner_score_v3", "runner_score_v2", "governed_projection_rating_v6", "confidence_adjusted_rating_v6", "projected_rating_v5_2"]) ?? projectionRatingValue(scoreBreakdownRunner), tone: "#7dd3fc" },\n                { label: "Distance", value: factorScoreValue(scoreBreakdownRunner, "DISTANCE") ?? firstNum(scoreBreakdownRunner.dna, ["distance_fit_score", "distance_profile_score"]), tone: "#34d399" },\n                { label: "Condition", value: factorScoreValue(scoreBreakdownRunner, "CONDITION") ?? firstNum(scoreBreakdownRunner.dna, ["condition_fit_score", "condition_profile_score"]), tone: "#f5c451" },\n                { label: "Class", value: factorScoreValue(scoreBreakdownRunner, "CLASS") ?? firstNum(scoreBreakdownRunner.dna, ["class_fit_score", "class_profile_score"]), tone: "#c084fc" },\n                { label: "Campaign", value: factorScoreValue(scoreBreakdownRunner, "CAMPAIGN") ?? firstNum(scoreBreakdownRunner.campaign, ["campaign_score", "campaign_risk_score"]), tone: "#38bdf8" },\n                { label: "Pace", value: factorScoreValue(scoreBreakdownRunner, "PACE"), tone: "#f8fafc" },\n                { label: "Connections", value: factorScoreValue(scoreBreakdownRunner, "CONNECTION") ?? connectionScoreValue(scoreBreakdownRunner), tone: "#7dd3fc" },\n                { label: "Market", value: factorScoreValue(scoreBreakdownRunner, "MARKET"), tone: "#f5c451" },\n                { label: "Confidence", value: firstNum(scoreBreakdownRunner.row, ["confidence_score", "edgeiq_confidence", "model_confidence", "confidence"]), tone: "#34d399" },\n              ]\n            : [];''', '''          const scoreBreakdownSource = scoreBreakdownRunner ? commandEvidenceSource(scoreBreakdownRunner) : undefined;\n          const scoreBreakdownRows = scoreBreakdownRunner\n            ? [\n                { label: "Overall", value: firstNum(scoreBreakdownSource, ["edgeiq_score_overall_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_overall_v2"])) },\n                { label: "Distance", value: firstNum(scoreBreakdownSource, ["edgeiq_score_distance_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_distance_v2"])) },\n                { label: "Condition", value: firstNum(scoreBreakdownSource, ["edgeiq_score_condition_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_condition_v2"])) },\n                { label: "Class", value: firstNum(scoreBreakdownSource, ["edgeiq_score_class_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_class_v2"])) },\n                { label: "Campaign", value: firstNum(scoreBreakdownSource, ["edgeiq_score_campaign_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_campaign_v2"])) },\n                { label: "Pace", value: firstNum(scoreBreakdownSource, ["edgeiq_score_pace_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_pace_v2"])) },\n                { label: "Connections", value: firstNum(scoreBreakdownSource, ["edgeiq_score_connections_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_connections_v2"])) },\n                { label: "Market", value: firstNum(scoreBreakdownSource, ["edgeiq_score_market_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_market_v2"])) },\n                { label: "Confidence", value: firstNum(scoreBreakdownSource, ["edgeiq_score_confidence_v2"]), tone: scoreToneValue(firstNum(scoreBreakdownSource, ["edgeiq_score_confidence_v2"])) },\n              ]\n            : [];''', 'score_breakdown_v2')
    rep('''                    { label: "Pace Map", count: paceCoverageCount },\n                    { label: "DNA", count: dnaCoverageCount },\n                    { label: "Hidden Gem", count: hiddenGemCoverageCount },\n                    { label: "Campaign", count: campaignCoverageCount },\n                    { label: "Connections", count: connectionsCoverageCount },\n                    { label: "History", count: historyCoverageCount },\n                    { label: "Trajectory", count: coverageCount((row) => hasEvidenceText(firstText(row.item.runnerTrajectory, ["trajectory_direction"], "")) || firstNum(row.item.runnerTrajectory, ["trajectory_score"]) !== null) },\n                    { label: "Market", count: marketCoverageCount },\n                    { label: "EDGEiQ Price", count: coverageCount((row) => fairPrice(row.item.row, row.item.bet) !== null) },''', '''                    { label: "Pace Map", count: paceCoverageCount },\n                    { label: "DNA", count: dnaCoverageCount },\n                    { label: "Hidden Gem", count: hiddenGemCoverageCount },\n                    { label: "Campaign", count: campaignCoverageCount },\n                    { label: "Connections", count: connectionsCoverageCount },\n                    { label: "History", count: historyCoverageCount },\n                    { label: "Trajectory", count: coverageCount((row) => hasEvidenceText(firstText(row.item.runnerTrajectory, ["trajectory_direction"], "")) || firstNum(row.item.runnerTrajectory, ["trajectory_score"]) !== null) },\n                    { label: "Market", count: marketCoverageCount },\n                    { label: "EDGEiQ Price", count: coverageCount((row) => fairPrice(row.item.row, row.item.bet) !== null) },''', 'footer_counts_existing_labels_preserved', required=False)
    if text==original: raise RuntimeError('No TSX changes applied')
    TSX.write_text(text, encoding='utf-8')
except Exception as e:
    status='COUNTS_SCORES_UI_BLOCKED_SAFE_NO_CHANGE'; err=str(e)
row={'status':status,'generated_at':datetime.now().isoformat(timespec='seconds'),'changes_applied':'|'.join(changes),'error':err}
wcsv(out,row); wcsv(sumout,row)
report.write_text('\n'.join(['EDGEiQ Counts/Scores UI Update V1','='*42,f'Status: {status}',f'Changes: {row["changes_applied"]}',f'Error: {err or "None"}'])+'\n', encoding='utf-8')
print(status); print('changes',row['changes_applied']); print('error',err)
