# Runner Sectionals Browser Evidence

A clean logged-out browser context returned HTTP `200` for the runner sectionals GraphQL request. Header-name evidence shows `x-api-key` present and cookie absent. Values are redacted and not stored.
